# Python Flask + HTML Project

This project is a minimal example of a Python backend using Flask, serving an HTML frontend.

## Structure
- `app.py`: Main Flask application
- `requirements.txt`: Python dependencies
- `templates/`: HTML files (Jinja2 templates)
- `static/`: Static assets (CSS, JS)

## Setup
1. Install dependencies:
   ```sh
   pip install -r requirements.txt
   ```
2. Run the app:
   ```sh
   python app.py
   ```
3. Open your browser at [http://127.0.0.1:5000](http://127.0.0.1:5000)

---

Feel free to expand this project with more routes, templates, and static files as needed.
